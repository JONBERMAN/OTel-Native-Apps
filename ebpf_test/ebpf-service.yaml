apiVersion: v1
kind: Service
metadata:
  labels:
    run: nginx-test
  name: ebpf-test
  namespace: playground
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    run: nginx-test
status:
  loadBalancer: {}
